{
  "name": "@pulse/agent",
  "version": "0.0.0",
  "private": true,
  "description": "Pulse desktop agent — minimal Electron tray tracker (Phase 1).",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && electron .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pulse/shared": "workspace:*",
    "active-win": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "^33.0.0"
  }
}
