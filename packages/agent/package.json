{
  "name": "@pulse/agent",
  "version": "0.0.0",
  "private": true,
  "description": "Pulse desktop agent (placeholder — built in Phase 1+).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pulse/shared": "workspace:*"
  }
}
